#include<stdio.h>

/**
 * main - prints without the use of puts command
 *
 * Return: 0 after execution
 *
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
}
